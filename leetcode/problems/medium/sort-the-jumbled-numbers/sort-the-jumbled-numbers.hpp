#ifndef SORT_THE_JUMBLED_NUMBERS_HPP
#define SORT_THE_JUMBLED_NUMBERS_HPP

// https://leetcode.com/problems/sort-the-jumbled-numbers/

#include <map>
#include <vector>

class SortTheJumbledNumbers {
public:
    std::vector<int> sortJumbled(const std::vector<int>& mapping, std::vector<int>& nums) const;
};

#endif